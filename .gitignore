# 运行时产物 —— 不入库
journal/*
!journal/.gitkeep
inbox/*
!inbox/.gitkeep
*.bak
*.bak.*

# 设计评审产物(截图)
shots/

# pack/deploy 产物(运行时,不入库)
.pack-stage/
.deploy-stage/
*.tar.gz

# python 字节码(lint.py/index.py 运行时生成)
__pycache__/
*.pyc

# lint/index 自测临时目录
.capture-selftest/
.promote-test-dest/
